import { RemoveUnderPipe } from './remove-under.pipe';

describe('RemoveUnderPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveUnderPipe();
    expect(pipe).toBeTruthy();
  });
});
