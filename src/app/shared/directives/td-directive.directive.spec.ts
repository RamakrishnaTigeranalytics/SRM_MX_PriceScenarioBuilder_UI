import { TdDirectiveDirective } from './td-directive.directive';

describe('TdDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TdDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
