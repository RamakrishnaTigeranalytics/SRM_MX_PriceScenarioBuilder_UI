import { ConvertTonnesPipe } from './convert-tonnes.pipe';

describe('ConvertTonnesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTonnesPipe();
    expect(pipe).toBeTruthy();
  });
});
