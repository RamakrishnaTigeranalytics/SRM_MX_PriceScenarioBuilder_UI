import { ComparePipePipe } from './compare-pipe.pipe';

describe('ComparePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ComparePipePipe();
    expect(pipe).toBeTruthy();
  });
});
